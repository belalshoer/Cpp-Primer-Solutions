#include <iostream>
int main() {
	// int i = 0;
	// (a) double* dp = &i; illegal cannot  make a double pointer point to an int object
	// (b) int *ip = i; illegal pointer doesn't get initalized and pointed to an object with an address
	// (c) int *p = &i; legal pointer points to the address of i
	return 0;
}
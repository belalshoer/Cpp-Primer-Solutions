#include <iostream>
int main() {
	// pointers are objects that allocate chunks of memory but refernces are not
	// one cannot vhange the object to ehivha reference is bound to but can change the address of the object to which the pointer points
	// we must initialize references at the time of defining them but we don't need to do the same with pointers

	return 0;
}
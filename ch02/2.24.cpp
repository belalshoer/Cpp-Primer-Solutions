#include <iostream>
int main() {
	// int i = 42; void *p = &i; long *lp = &i;
	// p is legal bacause a valid pointer can point o any other object
	// lp is illegal because pointers and objects cannot have different types.
	return 0;
}
#include<iostream>
int main() {
	//"int is good for most values, minimum = 16 bits and it is signed unless indicated otherwise"
	//short is usually too small, minimum = 16 bits
	// long often has the same size as int, minimum = 32 bits
	// long long is suitable for large values that cannot be stored in an int, minimum = 64 bits.
	// unsigned has 0 and positive numbers while, signed includes positive, negative numbers and 0
	// float and double minimum are 6 and 10 significant digits respectively. It is advised to use double at most cases
	// the cost of double-precision calculations versus single-precision is negligible and double-precision can be faster on some machines.
	return 0;
}
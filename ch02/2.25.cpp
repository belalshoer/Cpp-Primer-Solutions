#include <iostream>
int main() {
	// (a) int* ip, &r = ip; this question won't compile so it should be int* ip, i, &r = i;
	// now, ip is a pointer to int, i is an int, r is a refernce to i
	// (b) int i, * ip = 0; i is an int, ip is a pointer to int
	// (c) int* ip, ip2; ip is a pointer to int, ip2 is an int
	return 0;
}
#include<iostream>
#include <string>
#include <vector>
using std::vector;
using std::string;
using std::cout;
using std::cin;
using std::endl;
/*
(a) do{
 int v1, v2; 
 cout << "Please enter two numbers to sum:" ;
 if (cin >> v1 >> v2) cout << "Sum is: " << v1 + v2 << endl;
}while (cin);

calculating the sum of teo numbers. curly braces added to contain the do block


(b) do {
 // . . .
} while (int ival = get_response());
ival is defined inside the while condition

fix:
int ival;
do {
 // . . .
} while (ival = get_response());

(c) do {
 int ival = get_response();
 } while (ival); ival is undefined

 fix:
 int ival;
 do {
 ival = get_response();
 } while (ival);
*/

int main() {
	
	


	return 0;
}
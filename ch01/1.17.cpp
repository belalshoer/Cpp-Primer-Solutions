#include <iostream>
int main() {
	std::cout << "if the input values are all equal, the program will count the occurences of that number until we reach end-of-file form";
	std::cout << "if there are no duplicated values, then every number will have an occurence count of 1";
	return 0;
}
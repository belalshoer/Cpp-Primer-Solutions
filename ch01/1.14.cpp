#include <iostream>
int main() {
	std::cout << "while is good when one doesn'tknow the loop iterations on the other hand, for loop is suitablewhen one knows the number of iterations and it is beneficial as lets us define and initialize temporary variables, check for a condition and update the variables all in one line";
	return 0;
}
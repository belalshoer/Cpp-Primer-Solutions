#include<iostream>
#include <string>
#include <vector>
using std::vector;
using std::cout;
using std::cin;
using std::string;
using std::endl;
/*
(a) if (fval) float is converted to bool
(b) dval = fval + ival; ival is converted to float then the sum is converted to double
(c) dval + ival * cval; cval is coverted to int then the product is converted to double
*/
int main() {
	


	return 0;
}
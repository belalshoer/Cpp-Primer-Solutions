#include<iostream>
using std::cout;
using std::cin;
/*
in the logical AND the left operand is evaluated first and only if it is true the right one is evaluated
in the in the logical OR the left operand is evaluated first and only if it is flase the right one is evaluated
with the equality operator, the operands are evaluated then the equality check occurs
*/
int main() {

	return 0;
}
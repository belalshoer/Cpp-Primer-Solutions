#include<iostream>
using std::cout;
using std::cin;
/*
const char *cp = "Hello World";
if (cp && *cp)
we check if cp is not a nullptr so, in this case it is a const char* 
then we check if *cp is not null and in this case it is 'H'
so the if condition: (true && true) evaluates to true
*/
int main() {

	return 0;
}
#include<iostream>
#include <string>
#include <vector>
using std::vector;
using std::cout;
using std::cin;
using std::string;
using std::endl;
/*
* int i; double d; const string *ps; char *pc; void
*pv;
(a) pv = (void*)ps; static_cast<void *>(const_cast<string *> (ps));
(b) i = int(*pc); static_cast<int> (*pc);
(c) pv = &d; static_cast<void *>(&d);
(d) pc = (char*) pv; static_cast<char *> (pv);
*/
int main() {
	


	return 0;
}
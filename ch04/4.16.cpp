#include<iostream>
using std::cout;
using std::cin;
/*
(a) if (p = getPtr() != 0) -> (( p = getPtr()) != 0) without the parentheses we ill check if
getPtr() != 0 then assign the bool to p
(b) if (i = 1024) -> if (i == 1024) the other aversion checks if i is true after the assignment

*/
int main() {

	
	return 0;
}
#include<algorithm>
#include<numeric>
#include<iostream>
#include <string>
#include<vector>
#include <set>
#include<deque>
#include<list>
#include<forward_list>

using std::forward_list;
using std::deque;
using std::list;
using std::set;
using std::vector;

using std::string;
using std::cout;
using std::cin;
/*
well, yes the size changes but nit because of the algorithm for example, here the algorithm doesn't
change the size and the code is in error
vector<int> vec; list<int> lst; int i;
while (cin >> i)
	lst.push_back(i);
copy(lst.cbegin(), lst.cend(), vec.begin());
when we use a back inserter it is the one responsible for adding the elements as it uses
push_back() under the hood

*/

int main()
{
	

	return 0;
}

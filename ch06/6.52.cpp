#include<iostream>
#include <cassert>
#include <string>
#include <vector>
#include <array>
using std::vector;
using std::string;
using std::cout;
using std::cin;
using std::endl;
/*
(a) manip('a', 'z');
both arguments are matched through promotions to int so their rank is 3

(b) manip(55.4, dobj);
both arguments are matched through conversions to int so their rank is 4
*/

int main() {
	
}

#include<iostream>
#include <cassert>
#include <string>
#include <vector>
#include <array>
using std::vector;
using std::string;
using std::cout;
using std::cin;
using std::endl;
/*
the candidate functions are a set of overloadded functions which will be considered
in the call.

viable functions are a subset of candidate functions eheich have the same numbers of parameters
as of arguments in the call and which their type match ore can be converted to the
arguments' types
*/
int main() {
	

}

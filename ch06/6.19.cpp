#include<iostream>
#include <string>
#include <vector>

using std::vector;
using std::string;
using std::cout;
using std::cin;
using std::endl;
/*
(a) calc(23.4, 55.1); illegal, the number of arguments exceeds the number of parameters.
(b) count("abcda", 'a'); legal
(c) calc(66); legal
(d) sum(vec.begin(), vec.end(), 3.8); legal
*/

int main()
{

	return 0;
}


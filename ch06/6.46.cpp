#include<iostream>
#include <string>
#include <vector>
#include <array>
using std::vector;
using std::string;
using std::cout;
using std::cin;
using std::endl;
//no, because if it is constexpr then its body must be computed at compile time
//not run time but we have the ternary operator and size() in the body

int main() {

	return 0;

}
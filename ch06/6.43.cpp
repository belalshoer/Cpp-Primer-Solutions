#include<iostream>
#include <string>
#include <vector>
#include <array>
using std::vector;
using std::string;
using std::cout;
using std::cin;
using std::endl;
/*
(a) inline bool eq(const BigInt&, const BigInt&) {...} this would be put in a header
because the compiler needs its definition not just its declaration.

(b) void putValues(int *arr, int size); this one could be declared in the header and defined
in the source file.
*/

int main() {

	return 0;

}
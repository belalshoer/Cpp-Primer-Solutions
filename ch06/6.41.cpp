#include<iostream>
#include <string>
#include <vector>
#include <array>
using std::vector;
using std::string;
using std::cout;
using std::cin;
using std::endl;
/*
char *init(int ht, int wd = 80, char bckgrnd = ' ');
(a) init(); illegal, didn't provide an argument for the first parameter
(b) init(24,10); legal (24, 10, ' ')
(c) init(14, '*'); legal but char will be converted to int (14, 52 , ' ')

*/

int main() {
	return 0;

}
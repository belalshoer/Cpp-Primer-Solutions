#include<algorithm>
#include<iostream>
#include <string>
#include<vector>
#include <set>
#include<deque>
#include<list>
#include<forward_list>

using std::forward_list;
using std::deque;
using std::list;
using std::set;
using std::vector;

using std::string;
using std::cout;
using std::cin;


/*
vec.resize(100) will add 75 elements with value 0 to the back of vec
if we next wrote vec.resize(10) it will erase 90 elements from the back of vec
*/
int main()
{
	return 0;
}

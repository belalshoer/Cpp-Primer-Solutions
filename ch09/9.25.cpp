#include<algorithm>
#include<iostream>
#include <string>
#include<vector>
#include <set>
#include<deque>
#include<list>

using std::deque;
using std::list;
using std::set;
using std::vector;

using std::string;
using std::cout;
using std::cin;
/*
if they are equal then no elements will be erased
if elem 2 is the off-the-end iterator then the range from elem1 to to one before elem2 will be erased
if they are both the off-the-end iterator then no elements will be erased
*/


int main()
{
	return 0;
}

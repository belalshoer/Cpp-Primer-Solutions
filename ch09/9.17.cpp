#include<iostream>
#include <string>
#include <list>
#include <deque>
#include <vector>
#include <array>

using std::array;
using std::vector;
using std::list;
using std::deque;
using std::string;
using std::cout;
using std::cin;
// if (c1 < c2)
// the types have to be identical and support the < operator

int main()
{
		






	return 0;
}


#include<algorithm>
#include<iostream>
#include <string>
#include<vector>
#include <set>
#include<deque>
#include<list>
#include<forward_list>

using std::forward_list;
using std::deque;
using std::list;
using std::set;
using std::vector;

using std::string;
using std::cout;
using std::cin;

/*
the capacity is most likely to get doubled after exceeding it but it is system dependent so,
256 -> 1024
512 -> 1024
1000 -> 1024
1048 -> more than 1048
*/
int main()
{
	
	return 0;
}

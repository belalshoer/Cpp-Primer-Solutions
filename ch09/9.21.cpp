#include<algorithm>
#include<iostream>
#include <string>
#include<vector>
#include <set>
#include<deque>
#include<list>

using std::deque;
using std::list;
using std::set;
using std::vector;

using std::string;
using std::cout;
using std::cin;


// it will work the same way it worked but, it will be costly
// it will repeatedly move all the vector elements to insert an element at the front

int main()
{
	return 0;
}

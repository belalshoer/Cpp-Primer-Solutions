#include<iostream>
#include <vector>
#include <iterator>
using std::vector;
using std::cout;
using std::cin;
/*
p1 += p2 - p1; similar to p1 += n;
it is used to change the position that p1 points to given the difference (p2 - p1)
no, no values are illegal as long as they point to elements in the same array
*/
int main() {

	return 0;
}
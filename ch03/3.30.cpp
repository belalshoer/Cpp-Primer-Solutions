#include<iostream>
using std::cout;
using std::cin;
/*
constexpr size_t array_size = 10;
int ia[array_size];
for (size_t ix = 1; ix <= array_size; ++ix)
 ia[ix] = ix;

 the array si of size 10 in the last loop ix will be 10 but ia[10] would be out of range

*/
int main() {

	return 0;
}
#include <iostream>
#include <string>
using std::cout;
using std::cin;
using std::endl;
using std::string;
int main() {
	/*in get line we get all the line till we reach the end of the line so the whitespaces are ignored,
	with >> string inputting we get one word at a time so the whitespaces are not ignored*/
}
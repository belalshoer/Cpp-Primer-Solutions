#include<iostream>
using std::cout;
using std::cin;
/*
we must specify their sizes so they are not flexible and we can't add elements to them during run time
we can't copy them or assign them to other arrays

*/
int main() {
	
	return 0;
}
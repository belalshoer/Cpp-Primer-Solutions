#include <iostream>
#include <string>
#include <vector>
using std::cout;
using std::cin;
using std::string;
using std::endl;
using std::vector;
/*
(a) vector<int> v1; no elements nor values.
(b) vector<int> v2(10); 10 elements, values: 0
(c) vector<int> v3(10, 42); 10 elements, values: 42
(d) vector<int> v4{10}; 1 element, value: 10 
(e) vector<int> v5{10, 42}; 2 elements, values: 10, 42
(f) vector<string> v6{10}; 10 elements, values: empty string
(g) vector<string> v7{10, "hi"}; 10 elements, values: "hi"
*/
int main() {
	return 0;
}